// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: greetings.sql

package db

import (
	"context"
)

const createGreeting = `-- name: CreateGreeting :one
INSERT INTO greetings (name, message, emoji) VALUES ($1, $2, $3) RETURNING id, name, message, created_at, emoji
`

type CreateGreetingParams struct {
	Name    string
	Message string
	Emoji   string
}

func (q *Queries) CreateGreeting(ctx context.Context, arg CreateGreetingParams) (Greeting, error) {
	row := q.db.QueryRowContext(ctx, createGreeting, arg.Name, arg.Message, arg.Emoji)
	var i Greeting
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Message,
		&i.CreatedAt,
		&i.Emoji,
	)
	return i, err
}

const listGreetings = `-- name: ListGreetings :many
SELECT id, name, message, created_at, emoji FROM greetings ORDER BY created_at DESC LIMIT 20
`

func (q *Queries) ListGreetings(ctx context.Context) ([]Greeting, error) {
	rows, err := q.db.QueryContext(ctx, listGreetings)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Greeting
	for rows.Next() {
		var i Greeting
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Message,
			&i.CreatedAt,
			&i.Emoji,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
